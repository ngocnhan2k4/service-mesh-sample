apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: customers-resilience-test
  namespace: petclinic
spec:
  hosts:
  - customers-service
  http:
  - retries:
      attempts: 3
      retryOn: 5xx
    route:
    - destination:
        host: customers-service
        port:
          number: 80